#!/usr/bin/env bash
top_line="$(($1 + 1))"
x="$(($2))"
y="$(($3 - 1))"

export NVIM_APPNAME=kitty-nvim 
_nvim='/Users/mike/gitrepos/neovim/build/bin/nvim'
# shellcheck disable=2016
$_nvim +"$top_line" +"normal! zt" +"call cursor(line('.') + $y, $x)"
